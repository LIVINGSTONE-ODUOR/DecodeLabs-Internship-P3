import { useMemo, useState } from 'react';
import { faqs } from '../../utils/constants';
import FAQAccordion from '../features/FAQAccordion';
export default function FAQSection() { const [query,setQuery]=useState(''); const items=useMemo(()=>faqs.filter(f=>(f.question+f.answer+f.category).toLowerCase().includes(query.toLowerCase())),[query]); return <section className="section-pad bg-white dark:bg-slate-950"><div className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8"><div className="text-center"><p className="font-bold text-primary">FAQ</p><h2 className="mt-3 font-display text-4xl font-black">Questions leaders ask before we build.</h2></div><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search questions" className="mt-10 h-14 w-full rounded-2xl border border-slate-200 bg-slate-50 px-5 outline-none focus:border-primary dark:border-white/10 dark:bg-slate-900"/><div className="mt-6 grid gap-4">{items.map(item=><FAQAccordion key={item.question} item={item}/>)}</div></div></section>; }
