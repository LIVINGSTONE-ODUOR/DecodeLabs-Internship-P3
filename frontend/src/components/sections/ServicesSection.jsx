import { motion } from 'framer-motion';
import { services } from '../../utils/constants';
import ServiceCard from '../features/ServiceCard';
export default function ServicesSection() { return <section id="services" className="section-pad bg-white dark:bg-slate-950"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="mx-auto max-w-3xl text-center"><p className="font-bold text-primary">What we do</p><h2 className="mt-3 font-display text-4xl font-black sm:text-5xl">Digital services with technical depth and visual taste.</h2><p className="mt-5 text-lg leading-8 text-slate-600 dark:text-slate-300">Every offer is designed around measurable business outcomes, from faster support to higher conversion and cleaner operations.</p></div><motion.div variants={{ show:{ transition:{ staggerChildren:.08 } } }} initial="hidden" whileInView="show" viewport={{ once:true, amount:.2 }} className="mt-14 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{services.map((service)=><motion.div key={service.title} variants={{ hidden:{ opacity:0, y:24 }, show:{ opacity:1, y:0 } }}><ServiceCard service={service}/></motion.div>)}</motion.div></div></section>; }
