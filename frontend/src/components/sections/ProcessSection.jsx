import { motion } from 'framer-motion';
import { processSteps } from '../../utils/constants';
export default function ProcessSection() { return <section className="section-pad bg-slate-50 dark:bg-slate-900"><div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8"><div className="max-w-2xl"><p className="font-bold text-primary">How we work</p><h2 className="mt-3 font-display text-4xl font-black">A clear delivery path from idea to measurable impact.</h2></div><div className="relative mt-14"><div className="absolute left-6 top-0 hidden h-full w-px bg-gradient-to-b from-primary to-secondary md:block"/>{processSteps.map((step,i)=><motion.div key={step.title} initial={{ opacity:0, x:i%2?-24:24 }} whileInView={{ opacity:1, x:0 }} viewport={{ once:true }} className={`relative mb-8 grid gap-6 md:grid-cols-2 ${i%2 ? 'md:text-right' : ''}`}><div className={i%2 ? 'md:col-start-2' : ''}><div className="glass rounded-2xl p-6"><span className="inline-grid h-12 w-12 place-items-center rounded-xl bg-gradient-to-br from-primary to-secondary font-black text-white">{i+1}</span><h3 className="mt-4 text-2xl font-bold">{step.title}</h3><p className="mt-3 leading-7 text-slate-600 dark:text-slate-300">{step.description}</p></div></div></motion.div>)}</div></div></section>; }
